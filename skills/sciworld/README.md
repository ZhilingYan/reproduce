# skills/sciworld

占位目录:flat GRPO+OPSD 以 `+algorithm.sdar.privileged_source=gt` 运行时,
特权走环境 `info['extra.gt_plan']` 通道(整题 subgoal 骨架,
env_package/sciworld/goal_progress.py 的 render_gt_plan),本目录不被加载。
保留目录只为 `+algorithm.sdar.skills_dir=skills/sciworld` 配置项有落点
(search/textcraft 同款约定)。
